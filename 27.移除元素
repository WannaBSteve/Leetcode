class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int slowp=0,fastp=0,k=0;
        if(nums.size()==0)return 0;
        for(int i=0;i<=nums.size()-1;i++)
        {
            if(nums[i]!=val)nums[k++]=nums[i];
        }
        return k;
    }
};
//100 22    双指针法
